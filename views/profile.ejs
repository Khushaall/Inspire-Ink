<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .bg-custom {
            background-image: url('/images/back1.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
       
        .overlay {
            background-color: rgba(0, 0, 0, 0.4);
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start; 
            padding-top: 2rem; 
        }

        .container {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            width: 100%;
            max-width: 1200px; /* Adjust as needed */
            max-height: calc(100vh - 4rem); 
            overflow: hidden; 
            display: flex;
            flex-direction: column;
        }

        .header {
            position: sticky;
            top: 0;
            background-color: rgba(58, 58, 57, 0.596);
            padding: 1rem;
            z-index: 10;
            display: flex;
            gap: 1rem;
            align-items: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }

        .button-primary {
            background-color: #4a90e2;
            color: white;
            border-radius: 0.375rem;
            padding: 0.5rem 1rem;
            cursor: pointer;
            text-align: center;
            transition: background-color 0.3s;
        }

        .button-primary:hover {
            background-color: #357abd;
        }

        .post-card {
            background-color: #2d3748;
            border: 1px solid #4a5568;
            padding: 1rem;
            border-radius: 0.5rem;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.3);
        }

        .button-secondary {
            color: #e2e8f0;
            transition: color 0.3s;
        }

        .button-secondary:hover {
            color: #cbd5e0;
        }

        .logout-button {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background-color: #e53e3e; /* Red background */
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .logout-button:hover {
            background-color: #c53030; /* Darker red on hover */
        }

        .posts-container {
            overflow-y: auto;
            max-height: calc(100vh - 10rem); /* Adjust based on header height */
            padding: 1rem;
        }
    </style>
</head>

<body class="bg-custom">
    <div class="overlay">
        <a href="/logout" class="logout-button">Log Out</a>
        <div class="container text-white">
            <div class="header">
                <div class="w-16 h-16 rounded-full overflow-hidden">
                    <img class="w-full h-full object-cover" src="/images/<%= user.profilepic %>" alt="Profile Picture">
                </div>
                <h1 class="text-4xl font-bold ">Hello, <%= user.name %></h1>
            </div>
            <form method="post" action="/post" class=" mt-5">
                <textarea class="w-full p-4 bg-transparent border-2 border-zinc-800 rounded-lg resize-none text-white placeholder-gray-300"
                    name="content" placeholder="What's on your Mind"></textarea>
                <div class="flex justify-center mt-4 ">
                    <input class="button-primary px-20 py-2 text-lg mb-2" type="submit" value="Create">
                </div>
            </form>
            <div class=" posts-container">
                <h3 class="text-lg text-gray-300 mb-4 text-xl">Your Posts...</h3>
                <div class="flex gap-4 flex-wrap">
                    <% user.posts.reverse().forEach(function(post){ %>
                        <div class="post-card relative w-full md:w-1/3 lg:w-1/4 ">
                            <h4 class="text-xl font-semibold mb-2"><%= user.username %></h4>
                            <p><%= post.content %></p>
                            <div class=" flex gap-3 mt-3 mb-2 justify-between ">
                                <a href="/like/<%= post._id %>" class="button-secondary">
                                    <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                                </a>
                                <a href="/edit/<%= post._id %>" class="button-secondary">Edit</a>
                                <a href="/delete/<%= post._id %>" class="button-secondary">Delete</a>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
